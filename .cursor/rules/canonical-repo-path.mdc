---
description: 本项目唯一代码根目录（勿改错路径）
alwaysApply: true
---

# 代码根目录（必须遵守）

- **本仓库 / smart-erp 的唯一、权威代码根目录是：** `D:\smart\`
- 所有编辑、终端命令、`git` 操作、发版与版本号修改，**必须针对 `D:\smart`**，不要假设在其它路径。
- **禁止**在未说明的情况下修改以下路径下的同名项目（易与真仓库混淆）：  
  `c:\Users\水滴飞扬\.openclaw\workspace\smart\`  
  若用户明确要求改「OpenClaw 工作区里的那份」或给出其它路径，再按用户指示操作。
- 若当前 Cursor 工作区不是 `D:\smart`，应先提醒用户 **用 File → Open Folder 打开 `D:\smart`**，或确认用户是否就是要改其它副本。
